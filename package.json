{
  "name": "fracturelight-engine",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node ./scripts/dev-server.js || echo 'Add your dev server script'",
    "build:web": "echo 'Add build step to produce web playable build (zip or folder)'"
  },
  "dependencies": {}
}
